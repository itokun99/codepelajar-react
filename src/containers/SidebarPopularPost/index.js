import { React } from 'libraries';
import { Section, PopularPost } from 'components';

const SidebarPopularPost = () => (
  <Section className="sidebar-popular-post">
    <PopularPost />
  </Section>
);

export default SidebarPopularPost;
